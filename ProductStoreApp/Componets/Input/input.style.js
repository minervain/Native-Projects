import { StyleSheet } from "react-native";

export default StyleSheet.create({

    contain: {
        flexDirection: 'row',
        alignItems:"center",
    },

    input: {
        borderBottomColor: 'black',
        color: 'black',
        padding: 20,
        flex:1


    }
})